package com.individual;

import java.util.regex.Pattern;

public class Time {
    /*
    private final int hours;
    private final int minutes;

    Time() {
        this.hours = 0;
        this.minutes = 0;
    }

    Time(int hours, int minutes) {
        this.hours = hours;
        this.minutes = minutes;
    }

    public void set(int hours, int minutes) {
        new Time(hours, minutes);
    }

    public Time minus(Time time) {
        return new Time()
    }

    public Time plus(Time time) {

    }

    public boolean isTime(String time) {
        Pattern pattern = Pattern.compile("[0-9]{2}" + "[.:]{1}" + "[0-9]{2}");
        return time.matches(String.valueOf(pattern));
    }

     */
}
